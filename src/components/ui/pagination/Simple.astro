---
import TextBtn from "~/components/ui/buttons/TextBtn.astro";

const iconSize = 21;
const { page } = Astro.props;
---

<nav class="mb-4 flex justify-center md:mb-13" aria-label="pagination">
  <ul class="flex items-center">
    <li class="flex w-1/4 justify-end">
      {
        page.url.prev ? (
          <TextBtn
            href={page.url.prev}
            label="Previous"
            icon="material-symbols:arrow-left-alt"
            iconSize={iconSize}
            iconReverse={true}
            aria-label="go to previous page"
          />
        ) : (
          <TextBtn
            disabled={true}
            href=""
            label="Previous"
            icon="material-symbols:arrow-left-alt"
            iconSize={iconSize}
            iconReverse={true}
            aria-disabled="true"
          />
        )
      }
    </li>

    <li class="flex w-1/2 justify-center">
      <span class="px-4" aria-label="current page">
        Page {page.currentPage} of {page.lastPage}
      </span>
    </li>

    <li class="flex w-1/4 justify-end">
      {
        page.url.next ? (
          <TextBtn
            href={page.url.next}
            label="Next"
            icon="material-symbols:arrow-right-alt"
            iconSize={iconSize}
            aria-label="go to next page"
          />
        ) : (
          <TextBtn
            disabled={true}
            href=""
            label="Next"
            icon="material-symbols:arrow-right-alt"
            iconSize={iconSize}
            aria-disabled="true"
          />
        )
      }
    </li>
  </ul>
</nav>
