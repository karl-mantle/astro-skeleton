{
  "defaults": {
    "concurrency": 5,
    "standard": "WCAG2AA",
    "timeout": 30000,
    "level": "error",
    "runners": ["axe"],
    "chromeLaunchConfig": {
      "args": ["--no-sandbox", "--disable-setuid-sandbox"]
    }
  }
}
